export const route = { path: '/:instructorId/admin/students/:studentId', index: false, auth: 'admin', title: 'ملف الطالب' };
import React from 'react';
import { useParams } from 'react-router-dom';
import Avatar from '../../components/ui/Avatar';
import Badge from '../../components/ui/Badge';
import { adminStudents } from '../../mocks/adminMockData';
const Stat = ({ label, value }) => <div className="rounded-2xl bg-surface-muted p-4"><p className="text-xs text-ink-500">{label}</p><b className="mt-2 block text-xl text-ink-900">{value}</b></div>;
export default function StudentProfileDetailPage() { const { studentId } = useParams(); const student = adminStudents.find((s) => s.id === studentId) || adminStudents[0]; return <div className="mx-auto max-w-4xl space-y-6" dir="rtl"><section className="rounded-[var(--radius-xl)] bg-navy-900 p-7 text-white shadow-panel"><div className="flex flex-wrap items-center gap-5"><Avatar name={student.name} size="lg" /><div className="flex-1"><p className="text-sm text-brand-200">ملف الطالب</p><h1 className="mt-1 text-2xl font-extrabold">{student.name}</h1><p className="mt-1 text-sm text-white/65">{student.email} · {student.grade}</p></div><Badge variant="success">اشتراك نشط</Badge></div></section><div className="grid gap-4 sm:grid-cols-3"><Stat label="متوسط الدرجات" value={`${student.averageScore}%`} /><Stat label="الكورسات المسجلة" value="3" /><Stat label="آخر نشاط" value={new Date(student.lastActive).toLocaleDateString('ar-EG')} /></div><section className="rounded-[var(--radius-lg)] border border-surface-border bg-surface-default p-6 shadow-card"><h2 className="text-lg font-extrabold text-ink-900">التقدّم الدراسي</h2>{[{ name: 'أساسيات الجبر', progress: 65 }, { name: 'الهندسة المستوية', progress: 30 }, { name: 'التفاضل والتكامل', progress: 100 }].map((course) => <div key={course.name} className="mt-5"><div className="flex justify-between text-sm"><b className="text-ink-800">{course.name}</b><span className="text-brand-600">{course.progress}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-surface-muted"><div className="h-full rounded-full bg-brand-500" style={{ width: `${course.progress}%` }} /></div></div>)}</section></div>; }
