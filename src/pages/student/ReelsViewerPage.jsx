export const route = { path: '/:instructorId/reels', index: false, auth: 'student', title: 'مقاطع سريعة' };
import React, { useState } from 'react';
import { studentReels } from '../../mocks/studentMockData';
export default function ReelsViewerPage() { const [index, setIndex] = useState(0); const [liked, setLiked] = useState(false); const reel = studentReels[index]; const change = () => { setIndex((v) => (v + 1) % studentReels.length); setLiked(false); }; return <div className="mx-auto max-w-md" dir="rtl"><div className="overflow-hidden rounded-[var(--radius-xl)] bg-navy-900 shadow-panel"><div className="relative aspect-[9/15] bg-gradient-to-b from-brand-500 to-navy-900 p-6 text-white"><div className="absolute inset-0 opacity-20" style={{ backgroundImage: 'radial-gradient(white 1px, transparent 1px)', backgroundSize: '18px 18px' }} /><div className="relative flex h-full flex-col justify-end"><span className="mb-auto w-fit rounded-full bg-white/15 px-3 py-1 text-xs">مراجعة سريعة</span><p className="text-xs text-white/70">{index + 1} من {studentReels.length}</p><h1 className="mt-2 text-2xl font-extrabold">{reel.title}</h1><p className="mt-2 text-sm text-white/70">مقطع قصير يراجع معك أهم الأفكار قبل الاختبار.</p><div className="mt-6 flex items-center gap-3"><button onClick={() => setLiked(!liked)} className={`rounded-full px-4 py-2 text-sm font-bold ${liked ? 'bg-brand-400 text-navy-900' : 'bg-white/15'}`}>♥ {reel.likes + (liked ? 1 : 0)}</button><button onClick={change} className="rounded-full bg-white px-4 py-2 text-sm font-bold text-navy-900">التالي ←</button></div></div></div></div><div className="mt-4 flex justify-center gap-2">{studentReels.map((r, i) => <button key={r.id} onClick={() => { setIndex(i); setLiked(false); }} className={`h-2 rounded-full transition ${i === index ? 'w-7 bg-brand-500' : 'w-2 bg-surface-border'}`} />)}</div></div>; }
