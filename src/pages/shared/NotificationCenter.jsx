export const route = { path: '/:instructorId/notifications', index: false, auth: 'required', title: 'الإشعارات' };
import React, { useState } from 'react';
import Button from '../../components/ui/Button';
import { studentNotifications } from '../../mocks/studentMockData';
export default function NotificationCenter() { const [items, setItems] = useState(studentNotifications); const markAll = () => setItems((v) => v.map((x) => ({ ...x, read: true }))); return <div className="mx-auto max-w-3xl space-y-6" dir="rtl"><div className="flex items-end justify-between"><div><p className="text-sm font-bold text-brand-600">ابقَ على اطلاع</p><h1 className="text-2xl font-extrabold text-ink-900">الإشعارات</h1></div><Button variant="subtle" size="sm" onClick={markAll}>تحديد الكل كمقروء</Button></div><div className="overflow-hidden rounded-[var(--radius-xl)] border border-surface-border bg-surface-default shadow-card">{items.map((n) => <button key={n.id} onClick={() => setItems((v) => v.map((x) => x.id === n.id ? { ...x, read: true } : x))} className={`flex w-full items-start gap-4 border-b border-surface-border p-5 text-right last:border-0 ${n.read ? 'bg-white' : 'bg-brand-50/70'}`}><span className={`mt-1 h-3 w-3 shrink-0 rounded-full ${n.read ? 'bg-surface-border' : 'bg-brand-500'}`} /><span className="flex-1"><b className="text-sm text-ink-900">{n.title}</b><p className="mt-1 text-sm text-ink-600">{n.body}</p><small className="mt-2 block text-ink-500">{new Date(n.date).toLocaleDateString('ar-EG')}</small></span></button>)}</div></div>; }
